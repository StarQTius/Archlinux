#!/bin/fish

function call_doco
    docker compose $argv
    exit $status
end

if test (count $argv) -eq 0
    call_doco
end

if test $argv[1] = "run"
    set argv $argv[1] "--rm" $argv[2..]
end

call_doco $argv

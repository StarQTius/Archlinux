#!/bin/fish

function call_doco
  eval "docker compose $argv"
  exit "$status"
end

if test (count $argv) -eq 0
  call_doco
end

if string match "*run*" -- "$argv"
  set argv (string replace "run" "run --rm --remove-orphans" -- "$argv")
end

call_doco $argv

#!/usr/bin/fish

set -l n $argv[1]
set -l focused_output (swaymsg -t get_outputs | jq -r '.[] | select(.focused) | .name')

if test "$focused_output" = "eDP-1"
    # laptop screen: use workspace as-is
else
    # external monitor: offset by 10
    set n (math "$n + 10")
end

swaymsg workspace number $n

#!/bin/fish

while true
  sleep 60
  if test (date +'%w') -lt 5 -a 6 -le (date +'%k') -a (date +'%k') -lt 22
    logger "Timekeeper: allowing use"
  else if test (date +'%w') -ge 5 -a 6 -le (date +'%k') -a (date +'%k') -le 23
    logger "Timekeeper: allowing use"
  else
    poweroff
  end
end
